apiVersion: v1
kind: Service
metadata:
  name: {{ include "shopsync.fullname" . }}
  labels:
    {{- include "shopsync.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shopsync.selectorLabels" . | nindent 4 }}
